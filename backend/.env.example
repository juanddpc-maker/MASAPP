DATABASE_URL=postgresql://escuela_admin:escuela_dev_2026@localhost:5432/escuela_artes_marciales
JWT_SECRET=cambia_esto_por_un_secreto_largo_y_aleatorio
PORT=3000

# Correo (opcional en desarrollo - si lo dejas vacío, los correos solo se
# registran en la consola del backend, no se envían de verdad).
# Ejemplo con Gmail: crea una "contraseña de aplicación" en tu cuenta de Google
# (no tu contraseña normal) y úsala en SMTP_PASS.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu_correo@gmail.com
SMTP_PASS=tu_contraseña_de_aplicacion
SMTP_FROM=tu_correo@gmail.com
