# Dependencias
node_modules/

# Compilado
dist/
build/

# Variables de entorno (contienen contraseñas y claves, NUNCA se suben)
.env
.env.local
.env.*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Sistema operativo / editor
.DS_Store
.vscode/
*.swp

# NOTA IMPORTANTE: prisma/migrations/ NO va aquí.
# Esa carpeta SÍ debe subirse a GitHub, porque el deploy en producción
# (prisma migrate deploy) la necesita para aplicar el historial de cambios
# a la base de datos.
